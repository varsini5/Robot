import unittest

#(Your Robot and Terrain classes remain the same)

if __name__ == "__main__":
    # Add exit=False back to prevent Jupyter from exiting
    unittest.main(argv=['first-arg-is-ignored'], exit=False)
